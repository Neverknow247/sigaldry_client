# Godot 4+ specific ignores
.godot/
/android/

# This project stores its editor/import cache in a non-hidden "godot/" dir
# (config/use_hidden_project_data_directory=false in project.godot).
# It's fully generated by the editor and must not be committed.
/godot/

# Editor scene-save crash-recovery scratch files
*.tmp
